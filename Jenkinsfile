pipeline {
    any agent
    stages {
        stage("Build"){
            steps{
                script {
                    sh "npm install"
                    sh echo "Succesfull Install dependencies"
                }
            }
        } 
    }
}