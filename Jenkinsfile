pipeline {
    any agent
    stages {
        stage("Build"){
            steps{
                script {
                    // sh "npm install"
                    echo "Succesfull Installed dependencies"
                }
            }
        } 
    }
}